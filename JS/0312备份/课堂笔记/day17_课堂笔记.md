#### 昨日回顾

```
HTML常见标签
	文档相关标签
	文本相关标签
	格式相关标签
	列表相关标签
	图片媒体标签
	链接相关标签
	语义相关标签
	表单相关标签
	表格相关标签
CSS	
	选择器
		基本选择器
		扩展选择器
		伪类选择器
			特殊效果 :hover
			伪类结构选择器 :first-child
			伪元素	 ::before
	常见属性
		样式相关属性
			尺寸属性
			背景属性
			字体属性
			文本属性
			盒子模型
				边框
				内边距
				外边距
				
		布局相关属性
			浮动
			定位
			flex
			grid
			
js
	概念
		运行在客户端的脚本语言
	组成
		ECMAScript 基础语法
		DOM	文档对象模型
		BOM 浏览器对象模型
	基础语法
		数据类型
			基本数据类型
			对象数据类型
		变量
			定义
			初始化
		运算符
			算术运算符
			自增自减运算符
			比较运算符
			逻辑运算符
			赋值运算符
			三元运算符
		流程控制
			选择语句
				if语句
				if-else
				if-else-if
				switch
			循环语句
				for循环
				while循环
				do-while循环
		数组
			定义
			初始化
			二维数组
		函数
			作用
			定义
			调用
			参数
			返回值
		面向对象
			类和对象的概念
			类和对象的创建
			字面量方式创建对象
		内置对象
			Math
			Date
			Number 包装类(将基本数据类型包装成对象类型)
			Array
			String
			RegExp
			JSON
	DOM
    	获取元素
    		document.getElementById()
    		document.querSelector()
    		document.querSelectorAll()
    	操作元素
    		操作元素的标签体
    			元素对象.innerHTML
    		操作元素的属性
    			元素对象.属性名
    		操作元素的样式
    			元素对象.style.样式属性名
    			元素对象.className
    			元素对象.classList.add()  remove()
    	节点操作
    		属性
    			firstChild
    			lastChild
    			children
    			parentNode
    		方法
    			创建节点
    				document.createElement()
    			添加节点
    				element.appendChild()
    				element.insertBefore()
    			删除节点
    				父元素.removeChild(子元素)
    	常见事件
    		点击事件
    			click
    		焦点事件
    			blur
    			focus
    		鼠标移入移出事件
    			mouseover
    			mouseout
    		键盘事件
    			keydown
    			keyup
    			keypress
    		页面加载事件
    			load
    		表单元素内容改变事件
    			change
    		滚动事件
    			scroll 当文档被滚动时发生的事件
    		表单提交事件
    			submit	表单提交时触发
    		拖拽事件
    			drag	该事件在元素正在拖动时触发(自学)
    			
    事件高级
    	事件的两种注册方式
    		1,传统方式:on开头的属性赋值,本质就是设置元素的属性
    		2,监听注册方式:addEventListener,本质就是调用元素的方法
    	事件流
    		三个阶段
    			事件捕获阶段:网景提出的:根节点--> 事件源(从外层向内层)进行事件传播
    			当前目标阶段:事件到达具体目标的阶段
    			事件冒泡阶段:IE提出的:事件源-->根节点(从内层向外层)进行事件传播
```

#### 事件高级

##### 事件对象

```
概念
	事件发生之后,跟事件相关的一系列信息数据的集合,都会放到这个对象中,这个对象就是事件对象
    比如
        1,谁绑定了这个事件
        2,如果是鼠标触发的事件,可以得到鼠标相关的信息,比如鼠标的坐标
        3,如果是键盘触发的事件,可以得到键盘相关的信息,比如按下了哪个键
获取
	事件在触发的时候,都会产生事件对象,并且系统会以参数的形式传递给事件处理函数
	我们可以在事件处理函数中,声明一个参数,用来接收这个事件对象
	
作用
	1,可以获取当前事件的类型(e.type)
	2,在键盘事件中可以监听按下了哪个键(e.keyCode)
	3.可以获取具体点击了哪一个子元素(e.target)
	4,可以阻止某些标签的默认行为(e.preventDefault())
	5,可以阻止事件冒泡(e.stopPropagation())
```

##### 事件委托

```
概念	
	将事件委托给别人,代为处理
	不需要通过子元素来注册事件,而是委托给父元素
	本质就是给父元素添加事件,将事件处理函数绑定到父元素上执行
原因
	使用事件冒泡,当子元素的事件触发之后,会冒泡到父元素,然后执行父元素中的事件处理函数
好处
	1,只需要给父类绑定一个事件,没必要给每一个子元素绑定事件,提高了程序性能
	2,可以实现给新增的元素动态添加事件
```

##### 鼠标事件对象

```
目的
	为了获取鼠标在文档中的坐标
属性	
	client:表示鼠标在可视区域的x轴和y轴的坐标
	page:表示鼠标在页面文档的x轴和y轴的坐标(用的最多)
	screen:表示鼠标在电脑屏幕的x轴和y轴的坐标
```

#### BOM简介

```
概念
	浏览器对象模型（Browser Object Model (BOM)）
作用
	使 JavaScript 有能力与浏览器"对话"
	比如 弹框 新建窗口 跳转到其他页面 实现定时效果
组成
	Window窗口对象  表示浏览器中打开的窗口
	location对象	用于获得当前页面的地址 (URL)，并把浏览器重定向到新的页面。
	history对象	包含浏览器的历史
	screen 对象	包含有关用户屏幕的信息。
	navigator对象	包含有关访问者浏览器的信息。
操作思想
	将浏览器的各个组成部分抽成对象,以面向对象的方式来操作
	因为对象有属性和方法,可以提高开发效率
```

#### Window对象

```
概念	
	窗口对象,浏览器会为HTML文档创建一个 window 对象,包含了页面的变量,函数,弹框,document
方法
	弹框相关
		alert() 显示带有一段消息和一个确认按钮的警告框。
		prompt()	显示可提示用户输入的对话框。
		confirm()	显示带有一段消息以及确认按钮和取消按钮的对话框。
		
	窗口相关
		open()	打开一个新的浏览器窗口或查找一个已命名的窗口。
        close()	关闭浏览器窗口
        
	定时器相关
		定时一次
			setTimeout()
		循环定时
			setInterval()
```

#### Location对象

```
概念
	地址栏对象,可以实现刷新和跳转
属性
	href
		设置(可以实现跳转)
			location.href = 新的url地址
		获取
			location.href
方法
	reload() 刷新
```

#### History对象

```
方法
    back()	加载 history 列表中的前一个 URL
    forward()	加载 history 列表中的下一个 URL
    go()	加载 history 列表中的某个具体页面
```

